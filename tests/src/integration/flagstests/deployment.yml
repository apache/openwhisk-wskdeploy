application:
  name: wskdeploy-samples

  packages:
    triggerrule:
      name: helloWorldFlags
      namespace: guest
      actions:
        greeting:
          inputs:
            name: Bernie
            place: Vermont
