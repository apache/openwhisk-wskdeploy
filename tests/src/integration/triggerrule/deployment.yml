application:
  name: wskdeploy-samples

  package:
    triggerrule:
      name: helloWorldTriggerRule
      namespace: guest
      actions:
        greeting:
          inputs:
            name: Bernie
            place: Vermont
