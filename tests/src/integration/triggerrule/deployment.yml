application:
  name: wskdeploy-samples

  packages:
    triggerrule:
      name: helloWorldTriggerRule
      namespace: guest
      actions:
        greeting:
          inputs:
            name: Bernie
            place: Vermont
