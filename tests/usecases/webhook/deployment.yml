application:
  name: wskdeploy-samples
  namespace: _

  packages:
    git_webhook:
      credential: 23bc46b1-71f6-4ed5-8c54-816aa4f8c502:123zO3xZCLrMN6v2BKK1dXYFpXlPkccOFqm12CdAsMgRU4VrNZ9lyGVCGuMDGIwP

      feeds:
        webhook:
          #we treat the inputs as parameters besides --feed.
          inputs:
            # Below values are fake values of username, repository, accessToken, events and endpoint.
	    # You need to replace them with the real data.
            username: github_id		# user name in github.com
            repository: https://github.com/XXX/XXX.git		# the url of git repository 
            accessToken: 8854acesdf	# the access token of github
            events: PUSH		# the event of github, e.g. PUSH
            endpoint: myopenwhisk.net	#the name or IP of the OpenWhisk server
