application:
  name: wskdeploy-samples
  namespace: _				# TODO: REPLACE IT with the namespace used in OpenWhisk server
  credential: c82b7b9d-296a-4b14	# TODO: REPLACE IT with the credential to access OpenWhisk server

  packages:
    git_webhook:
      feeds:
        webhook:
          #we treat the inputs as parameters besides --feed.
          inputs:
            # Below values are fake values of username, repository, accessToken, events and endpoint.
            # TODO: REPLACE them with the real data.
            username: github_id				# user name in github.com
            repository: https://github.com/id/XXX.git	# the url of git repository 
            accessToken: 8854acesdf			# the access token of github
            events: PUSH				# the event of github, e.g. PUSH, FORK, etc
            endpoint: myopenwhisk.net			# the name or IP of the OpenWhisk server
